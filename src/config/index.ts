import {
  PORT,
  DB_URL,
  DB_NAME,
  SALT_COUNT,
  SECRET_KEY,
  TEMP_FOLDER_PATH,
  AVATARS_FOLDER_PATH,
  MAX_AVATAR_SIZE,
  AVATAR_PX_SIZE,
} from "./config";

export {
  PORT,
  DB_URL,
  DB_NAME,
  SALT_COUNT,
  SECRET_KEY,
  TEMP_FOLDER_PATH,
  AVATARS_FOLDER_PATH,
  MAX_AVATAR_SIZE,
  AVATAR_PX_SIZE,
};
